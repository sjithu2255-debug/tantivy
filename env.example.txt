# --- required ---
DATABASE_URL=postgres://user:pass@host:5432/dayledger
PGSSL=require
SESSION_SECRET=generate_with__openssl_rand_base64_48
APP_URL=https://dayledger.yourdomain.com
TZ_NAME=Asia/Kolkata
NODE_ENV=production
PORT=8080

# --- first admin (used once by npm run seed:admin, then delete) ---
ADMIN_NAME=Jithu Sreekumar
ADMIN_EMAIL=you@company.com
ADMIN_PASSWORD=

# --- reminders: set email, Slack, or both. Neither = dry run to console ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Day Ledger <noreply@company.com>"
SLACK_WEBHOOK_URL=

REMIND_EVERY_HOURS=3
REMIND_HORIZON_HOURS=72
IDLE_CLOSE_AT=19:00
